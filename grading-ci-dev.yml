stages:
  - grade

build-job:
  stage: grade
  image: "$TESTING_DOCKER_IMAGE"
  script:
    - echo "$CI_PROJECT_NAME"
    - pwd
    - git clone https://git.ucsc.edu/etekin/gradepy.git
    - cd pa1
    - python3 ../gradepy/grade.py --grade --pa 1 -v debug --user "$CI_PROJECT_NAME"
  rules:
    - if: $CI_PIPELINE_SOURCE == 'push'
